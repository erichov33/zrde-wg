// Centralize all application-related utilities
import type { ApplicationStatus } from "@/lib/types/application"

export const getStatusIcon = (status: ApplicationStatus) => { /* ... */ }
export const formatApplicationDate = (date: string) => { /* ... */ }
export const calculateRiskScoreColor = (score: number) => { /* ... */ }